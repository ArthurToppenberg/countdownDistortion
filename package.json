{
  "name": "countdown-distortion",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules .turbo",
    "send-email": "tsx --env-file=.env scripts/email/sendCountdownEmail.ts"
  },
  "dependencies": {
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "rimraf": "^6.0.1",
    "tsx": "^4.0.0",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.6.5"
}
