{
  "name": "countdown-distortion",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules .turbo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.6.5"
}
